# CodeGenerator

## Overview
A DSPy Module used by the `AgentLoop`.

## Responsibilities
- Utilizes the `CodeGenerationSignature` (`objective`, `feedback` -> `python_code`).
- Generates self-contained scripts to solve tasks.

# IVectorStore

## Overview
The Hexagonal Port defining semantic memory and embeddings storage.

## Responsibilities
- `add_document(collection_name, document_id, text, metadata)`
- `search(collection_name, query, top_k)`

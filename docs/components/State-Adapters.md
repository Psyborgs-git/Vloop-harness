# State Adapters

## Overview
The collection of concrete implementations for data persistence ports (`IRelationalDB`, `IVectorStore`).

## Responsibilities
- Handle actual data storage. Includes `SQLiteAdapter` and `DummyVectorStoreAdapter`.

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: vloop
      POSTGRES_USER: vloop
      POSTGRES_PASSWORD: vloop
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data:
